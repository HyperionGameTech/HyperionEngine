cmake_minimum_required(VERSION 3.5)
cmake_policy(SET CMP0091 NEW)

project(hyperion-sample CXX C)

# Each sample subdirectory with a CMakeLists.txt defines its own target.
add_subdirectory(DefaultGame)
