# Hey!

If you got here, you should know that the DX12 backend is non functional as of 1/29/2026.
Starting the engine with HYP_DX12 instead of HYP_VULKAN will crash the engine pretty much instantaneously (if it even currently compiles).

Use the Vulkan implementation now as I work on rewriting all GLSL shaders to HLSL then we should be ready to start rocking and rolling with actually implementing the backend. 
